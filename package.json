{
  "name": "ironmart-biometric-agent",
  "version": "2.0.0",
  "description": "Standalone office-LAN agent that polls the ESSL K30 biometric device, pushes punch logs to the IronMart HRMS backend, and enrolls/removes employees on the device on HRMS's behalf. Runs outside the VPS Docker Compose stack — see README.md.",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "zkteco-js": "^1.7.2"
  }
}
